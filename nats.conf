# Client port of 4222 on all interfaces
port: 4222

# HTTP monitoring port
monitor_port: 8222

authorization {
   users: [
        # bcrypt hash can be generated using: docker run --rm -it natsio/nats-box nats server passwd
        {user: mainuser, password: "bcrypt-hash"},
    ]
}
